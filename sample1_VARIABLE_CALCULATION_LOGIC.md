# 学生点名系统 - 变量计算逻辑详解

## 📊 变量分类

### 一、基础变量（直接存储）

这些变量直接存储在学生对象中，通过操作直接修改：

#### 1. `purchasedClasses` - 已购堂数
- **类型**: Number
- **初始值**: 用户输入（添加学生时）
- **修改方式**: 
  - 添加学生时设置
  - 通过 `editPurchased()` 函数修改
- **计算逻辑**: 无（直接存储）
- **限制**: ≥ 0

#### 2. `scheduledClasses` - 已定日子课堂
- **类型**: Number
- **初始值**: 0
- **修改方式**:
  - **增加**: `scheduleClass()` - 定日子课堂时 +1
  - **减少**: 
    - `markAbsent()` - 标记缺席时 -1
    - `requestLeave()` - 请假时 -1
- **计算逻辑**: 无（直接存储）
- **限制**: 
  - `scheduledClasses ≥ attendedBooked`（不能少于已出席）
  - `scheduledClasses + totalAttendance ≤ purchasedClasses`

#### 3. `attendedBooked` - 已出席（已定日子）
- **类型**: Number
- **初始值**: 0
- **修改方式**:
  - **增加**: `markAttended()` - 标记出席时 +1
- **计算逻辑**: 无（直接存储）
- **限制**: 
  - `attendedBooked ≤ scheduledClasses`（不能超过已定日子课堂）
  - `attendedBooked + attendedMakeup ≤ purchasedClasses`（总出席不能超过已购堂数）

#### 4. `absences` - 缺席次数
- **类型**: Number
- **初始值**: 0
- **修改方式**:
  - **增加**: `markAbsent()` - 标记缺席时 +1
- **计算逻辑**: 无（直接存储）
- **限制**: 无

#### 5. `leaveRequests` - 请假次数
- **类型**: Number
- **初始值**: 0
- **修改方式**:
  - **增加**: `requestLeave()` - 请假时 +1
- **计算逻辑**: 无（直接存储）
- **限制**: 无

#### 6. `bookedMakeup` - 已约补堂
- **类型**: Number
- **初始值**: 0
- **修改方式**:
  - **增加**: 
    - `bookOvertimeClass()` - 预约加时时 +1
    - `bookRescheduleClass()` - 预约调堂时 +1
- **计算逻辑**: 无（直接存储）
- **限制**: 
  - `bookedMakeup ≤ leaveRequests`（已约补堂不能超过请假次数）

#### 7. `attendedMakeup` - 补堂已出席
- **类型**: Number
- **初始值**: 0
- **修改方式**:
  - **增加**: `markMakeupAttended()` - 标记补堂出席时 +1
- **计算逻辑**: 无（直接存储）
- **限制**: 
  - `attendedMakeup ≤ bookedMakeup`（补堂已出席不能超过已约补堂）
  - `attendedBooked + attendedMakeup ≤ purchasedClasses`（总出席不能超过已购堂数）

---

### 二、派生变量（计算得出）

这些变量不在学生对象中存储，而是在渲染时动态计算：

#### 1. `unscheduled` - 未定日子课堂
```javascript
unscheduled = purchasedClasses - scheduledClasses
```

**计算位置**: `renderStudents()` 函数中（第 357 行）

**逻辑说明**:
- 已购堂数减去已定日子课堂，得到还未安排日期的课堂数量
- **示例**: 
  - 已购 10 堂，已定 3 堂 → 未定 7 堂
  - 已购 10 堂，已定 10 堂 → 未定 0 堂

**使用场景**:
- 显示在 UI 上
- `scheduleClass()` 操作前检查：`unscheduled > 0`

---

#### 2. `scheduledNotAttended` - 已定日子课堂（未出席）
```javascript
scheduledNotAttended = scheduledClasses - attendedBooked
```

**计算位置**: `renderStudents()` 函数中（第 358 行）

**逻辑说明**:
- 已定日子课堂减去已出席，得到已安排但还未出席的课堂数量
- **示例**:
  - 已定 5 堂，已出席 2 堂 → 未出席 3 堂
  - 已定 5 堂，已出席 5 堂 → 未出席 0 堂

**使用场景**:
- 显示在 UI 上
- `markAttended()` 操作前检查：`scheduledNotAttended > 0`
- `markAbsent()` 操作前检查：`scheduledNotAttended > 0`

---

#### 3. `unbookedMakeup` - 未约补堂
```javascript
unbookedMakeup = leaveRequests - bookedMakeup
```

**计算位置**: `renderStudents()` 函数中（第 359 行）

**逻辑说明**:
- 请假次数减去已约补堂，得到请假但还未预约补堂的数量
- **示例**:
  - 请假 3 次，已约 1 次 → 未约 2 次
  - 请假 3 次，已约 3 次 → 未约 0 次

**使用场景**:
- 显示在 UI 上
- `bookOvertimeClass()` 操作前检查：`unbookedMakeup > 0`
- `bookRescheduleClass()` 操作前检查：`unbookedMakeup > 0`

---

#### 4. `totalAttendance` - 总出席
```javascript
totalAttendance = attendedBooked + attendedMakeup
```

**计算位置**: 多个函数中动态计算

**逻辑说明**:
- 已出席（已定日子）加上补堂已出席，得到总出席数
- **示例**:
  - 已出席 5 堂，补堂已出席 2 堂 → 总出席 7 堂

**使用场景**:
- 显示在 UI 上（第 432 行）
- `scheduleClass()` 验证：`scheduledClasses + 1 + totalAttendance ≤ purchasedClasses`
- `markAttended()` 验证：`totalAttendance + 1 ≤ purchasedClasses`
- `markMakeupAttended()` 验证：`totalAttendance + 1 ≤ purchasedClasses`

---

#### 5. `attendanceRate` - 出席率
```javascript
if (scheduledClasses > 0) {
    attendanceRate = Math.round((attendedBooked / scheduledClasses) * 100)
} else {
    attendanceRate = 0
}
```

**计算位置**: `renderStudents()` 函数中（第 360-362 行）

**逻辑说明**:
- 已出席除以已定日子课堂，乘以 100，四舍五入
- 如果没有已定日子课堂，出席率为 0
- **示例**:
  - 已定 10 堂，已出席 8 堂 → 出席率 80%
  - 已定 5 堂，已出席 3 堂 → 出席率 60%
  - 已定 0 堂 → 出席率 0%

**颜色编码**:
- `≥ 80%`: 绿色（优秀）
- `≥ 60%`: 黄色（良好）
- `< 60%`: 红色（需关注）

**使用场景**:
- 显示在学生卡片顶部（第 370 行）

---

## 🔄 操作对变量的影响

### 操作流程图

```
已购堂数 (purchasedClasses)
    │
    ├─→ 未定日子课堂 (unscheduled)
    │       │
    │       └─→ [定日子课堂] → 已定日子课堂 (scheduledClasses)
    │                           │
    │                           ├─→ 已定日子课堂（未出席）(scheduledNotAttended)
    │                           │       │
    │                           │       ├─→ [标记出席] → 已出席 (attendedBooked)
    │                           │       │
    │                           │       └─→ [标记缺席] → 缺席 (absences)
    │                           │                    └─→ 已定日子课堂 -1
    │                           │
    │                           └─→ [请假] → 请假次数 (leaveRequests)
    │                                           └─→ 已定日子课堂 -1
    │                                               │
    │                                               └─→ 未约补堂 (unbookedMakeup)
    │                                                       │
    │                                                       ├─→ [预约加时] → 已约补堂 (bookedMakeup)
    │                                                       │
    │                                                       └─→ [预约调堂] → 已约补堂 (bookedMakeup)
    │                                                               │
    │                                                               └─→ [标记补堂出席] → 补堂已出席 (attendedMakeup)
    │
    └─→ 总出席 (totalAttendance) = 已出席 + 补堂已出席
```

---

### 详细操作影响表

| 操作 | 影响的变量 | 变化 | 验证条件 |
|------|-----------|------|---------|
| **定日子课堂** | `scheduledClasses` | +1 | `unscheduled > 0`<br>`scheduledClasses + 1 + totalAttendance ≤ purchasedClasses` |
| **标记出席** | `attendedBooked` | +1 | `scheduledNotAttended > 0`<br>`totalAttendance + 1 ≤ purchasedClasses` |
| **标记缺席** | `scheduledClasses` | -1 | `scheduledNotAttended > 0`<br>`scheduledClasses - 1 ≥ attendedBooked` |
| | `absences` | +1 | |
| **请假** | `leaveRequests` | +1 | `scheduledClasses - 1 ≥ attendedBooked` |
| | `scheduledClasses` | -1 | |
| **预约加时** | `bookedMakeup` | +1 | `unbookedMakeup > 0` |
| **预约调堂** | `bookedMakeup` | +1 | `unbookedMakeup > 0` |
| **标记补堂出席** | `attendedMakeup` | +1 | `attendedMakeup < bookedMakeup`<br>`totalAttendance + 1 ≤ purchasedClasses` |
| **修改已购堂数** | `purchasedClasses` | = 新值 | 新值 ≥ 0 |

---

## 📐 计算公式汇总

### 派生变量计算公式

```javascript
// 1. 未定日子课堂
unscheduled = purchasedClasses - scheduledClasses

// 2. 已定日子课堂（未出席）
scheduledNotAttended = scheduledClasses - attendedBooked

// 3. 未约补堂
unbookedMakeup = leaveRequests - bookedMakeup

// 4. 总出席
totalAttendance = attendedBooked + attendedMakeup

// 5. 出席率
attendanceRate = scheduledClasses > 0 
    ? Math.round((attendedBooked / scheduledClasses) * 100) 
    : 0
```

---

## 🔒 约束条件汇总

### 1. 已定日子课堂约束
```javascript
scheduledClasses ≥ attendedBooked
```
**说明**: 已定日子课堂不能少于已出席数

### 2. 总出席约束
```javascript
totalAttendance ≤ purchasedClasses
```
**说明**: 总出席（已出席 + 补堂已出席）不能超过已购堂数

### 3. 已出席约束
```javascript
attendedBooked ≤ scheduledClasses
```
**说明**: 已出席不能超过已定日子课堂

### 4. 补堂已出席约束
```javascript
attendedMakeup ≤ bookedMakeup
```
**说明**: 补堂已出席不能超过已约补堂

### 5. 已约补堂约束
```javascript
bookedMakeup ≤ leaveRequests
```
**说明**: 已约补堂不能超过请假次数（逻辑上，但代码中未明确验证）

### 6. 已定日子课堂和总出席总和约束
```javascript
scheduledClasses + totalAttendance ≤ purchasedClasses
```
**说明**: 已定日子课堂和总出席的总和不能大于已购堂数

---

## 💡 计算逻辑示例

### 示例场景：学生小明

**初始状态**:
- 已购堂数: 10
- 其他字段: 0

**操作序列**:

1. **定日子课堂 5 次**
   - `scheduledClasses`: 0 → 5
   - `unscheduled`: 10 → 5

2. **标记出席 3 次**
   - `attendedBooked`: 0 → 3
   - `scheduledNotAttended`: 5 → 2
   - `attendanceRate`: 0% → 60%

3. **请假 1 次**
   - `leaveRequests`: 0 → 1
   - `scheduledClasses`: 5 → 4
   - `scheduledNotAttended`: 2 → 1
   - `unbookedMakeup`: 0 → 1

4. **预约加时 1 次**
   - `bookedMakeup`: 0 → 1
   - `unbookedMakeup`: 1 → 0

5. **标记补堂出席 1 次**
   - `attendedMakeup`: 0 → 1
   - `totalAttendance`: 3 → 4

**最终状态**:
- `purchasedClasses`: 10
- `scheduledClasses`: 4
- `attendedBooked`: 3
- `absences`: 0
- `leaveRequests`: 1
- `bookedMakeup`: 1
- `attendedMakeup`: 1
- `unscheduled`: 6 (计算)
- `scheduledNotAttended`: 1 (计算)
- `unbookedMakeup`: 0 (计算)
- `totalAttendance`: 4 (计算)
- `attendanceRate`: 75% (计算)

---

## 🎯 关键计算点

### 1. 渲染时计算
所有派生变量都在 `renderStudents()` 函数中计算，确保每次渲染时都是最新值。

### 2. 操作前验证
每个操作函数都会在修改前验证相关约束条件，防止数据不一致。

### 3. 状态保存
每次操作后都会调用 `saveState()` 保存当前状态到历史记录，支持撤销/重做。

---

## ⚠️ 注意事项

1. **派生变量不存储**: 所有计算得出的变量都不存储在学生对象中，只在需要时计算。

2. **约束验证顺序**: 操作函数中的验证顺序很重要，先检查前置条件，再检查约束条件。

3. **整数运算**: 所有变量都是整数，不存在小数。

4. **出席率计算**: 当 `scheduledClasses = 0` 时，出席率为 0%，避免除以零错误。

5. **总出席计算**: 在多个地方需要计算 `totalAttendance`，确保使用相同的公式。

---

**总结**: 系统使用 7 个基础变量存储数据，5 个派生变量动态计算，通过 8 种操作修改状态，并遵循 6 个约束条件确保数据一致性。
























